name: dropdown_plus_bloc
description: "Customizable Flutter dropdowns with BLoC/Cubit integration: searchable single-select and multi-select with chips, offline caching, and theming."
version: 0.1.4
homepage: https://github.com/LidhinCS/dropdown_plus
repository: https://github.com/LidhinCS/dropdown_plus
issue_tracker: https://github.com/LidhinCS/dropdown_plus/issues

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ">=8.0.0 <10.0.0"

dev_dependencies:
  bloc_test: ">=9.0.0"
  flutter_lints: ">=3.0.0"
  flutter_test:
    sdk: flutter

flutter:
